{% extends "TroiswaBackBundle::layout_admin.html.twig" %}

{% block title %}
{% endblock %}

{% block h1 %}
    Edition categorie
{% endblock %}

{% block fil_ariane %}
    {{ parent() }}
    <li class="active">
        <i class="fa fa-dashboard"></i>Editer categorie
    </li>

{% endblock %}

{% block content %}

   {# {{ form(formUdaptCategory) }} #}

    {{ form_start(formUdaptCategory, {attr : {class : 'form-horizontal'} } ) }}

    <div class="form-group">
        {{ form_label(formUdaptCategory.title,"Titre",{label_attr : {class : "col-sm-2"}}) }}
        {{ form_errors(formUdaptCategory.title, {attr:{ class:"form-erreur"}}) }}
        <div class="col-sm-10">
            {{ form_widget(formUdaptCategory.title, {attr:{ class:"form-control"}}) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(formUdaptCategory.description,"Description",{label_attr : {class : "col-sm-2"}}) }}
        {{ form_errors(formUdaptCategory.description, {attr:{ class:"form-erreur"}}) }}

        <div class="col-sm-10">
            {{ form_widget(formUdaptCategory.description, {attr:{ class:"form-control"}}) }}
        </div>
    </div>

    {{ form_end(formUdaptCategory) }}

{% endblock %}