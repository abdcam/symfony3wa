{% extends "TroiswaBackBundle::layout_admin.html.twig" %}

{% block title %}
{% endblock %}

{% block h1 %}
    Editer la marque des produits
{% endblock %}

{% block fil_ariane %}
    {{ parent() }}
    <li class="active">
        <i class="fa fa-dashboard"></i>Editer marque produit
    </li>

{% endblock %}

{% block content %}

    {{ form_start(formBrand, {attr : {class : 'form-horizontal'} } ) }}

<div class="form-group">
    {{ form_label(formBrand.title,"Marque",{label_attr : {class : "col-sm-2"}}) }}
    {{ form_errors(formBrand.title, {attr:{ class:"form-erreur"}}) }}
    <div class="col-sm-10">
        {{ form_widget(formBrand.title, {attr:{ class:"form-control"}}) }}
    </div>
</div>

<div class="form-group">
    {{ form_label(formBrand.description,"Description",{label_attr : {class : "col-sm-2"}}) }}
    {{ form_errors(formBrand.description, {attr:{ class:"form-erreur"}}) }}

    <div class="col-sm-10">
        {{ form_widget(formBrand.description, {attr:{ class:"form-control"}}) }}
    </div>
</div>

<div class="form-group">
   {{ form_widget(formBrand.button,{label:"Send"}) }}
</div>

{{ form_end(formBrand) }}

{% endblock %}